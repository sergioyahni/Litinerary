# Litinerary mock-only staging backend configuration.
# Placeholder-only; do not commit real secrets.
# Copy values into the staging environment manager only after replacing example
# hostnames with the real staging URLs. Do not create or commit a real
# .env file with secrets. Real providers and external calls remain disabled.

APP_ENV=staging
DEBUG=false
ENABLE_ADMIN_ROUTES=false
ENABLE_DEBUG_ROUTES=false
ENABLE_MOCK_SERVICES=true
ENABLE_REAL_LLM=false
ENABLE_REAL_VECTOR_DB=false
ENABLE_REAL_POI_PROVIDER=false
ENABLE_REAL_ROUTING=false
ENABLE_REAL_TICKETING=false
ENABLE_REAL_TTS=false
ENABLE_AFFILIATE_LINKS=false
# Managed-auth JWKS/provider metadata validation is the only approved external
# call for this beta template. Keep all real product providers disabled below.
ALLOW_EXTERNAL_CALLS=true
ENABLE_INTEGRATION_TESTS=false
ENABLE_STAGED_INTERNAL_LLM_TESTING=false
ENABLE_INTERNAL_ACCESS_GATE=false
EXTERNAL_CALL_ALLOWED_ENVIRONMENTS=beta,staging
ENABLE_AUTH=true
AUTH_PROVIDER=auth0
# Required for deployed Auth0 JWT validation. These staging placeholders must
# map to the same Auth0 API audience used by the staging SPA.
AUTH_JWT_ISSUER=https://<auth0-staging-domain>/
AUTH_JWT_AUDIENCE=<auth0-staging-api-audience>
AUTH_JWT_ALGORITHMS=RS256
AUTH_JWKS_URL=https://<auth0-staging-domain>/.well-known/jwks.json
AUTH_PROVIDER_METADATA_URL=https://<auth0-staging-domain>/.well-known/openid-configuration
AUTH_USER_ID_CLAIM=sub
AUTH_ROLES_CLAIM=roles
AUTH_SUBSCRIPTION_CLAIM=subscription_status
AUTH_EMAIL_CLAIM=email
AUTH_DISPLAY_NAME_CLAIM=name
AUTH_REQUIRED_FOR_USER_FEATURES=true
AUTH_ALLOW_DEV_USER_FALLBACK=false
# Must exactly match the staging frontend origin.
# Example only; replace in the deployed environment, not in source control.
CORS_ALLOWED_ORIGINS=https://staging.litinerary.example

# Required in deployed profiles. Must point at a non-production staging
# database whose Alembic revision is upgraded to head before application boot.
LITINERARY_DATABASE_URL=<staging-managed-database-url-from-secret-store>

LITINERARY_AI_PROVIDER=fake
LLM_PROVIDER=fake
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL_NAME=gpt-4.1-mini
LLM_TIMEOUT_SECONDS=20
LLM_MAX_TOKENS=1200
LLM_MAX_INPUT_CHARS=12000
LLM_MAX_OUTPUT_TOKENS=1200
LLM_MAX_RETRIES=0
LLM_MONTHLY_BUDGET_USD=
LLM_MAX_LIVE_CALLS_PER_REQUEST=4
LLM_DAILY_LIVE_REQUEST_CEILING=0
LLM_DAILY_ESTIMATED_SPEND_CEILING_USD=0
LLM_LATENCY_ALERT_THRESHOLD_MS=5000
LLM_ERROR_RATE_ALERT_THRESHOLD_PERCENT=10
# Current staging template remains mock-only; live LLM traffic is not approved.
LLM_ALLOWED_ENVIRONMENTS=production

LITINERARY_VECTOR_PROVIDER=fake
VECTOR_DB_PROVIDER=fake
VECTOR_DB_API_KEY=
LITINERARY_VECTOR_DIMENSION=16
LITINERARY_VECTOR_STORE_PATH=
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION_PREFIX=litinerary
QDRANT_TIMEOUT_SECONDS=5

LITINERARY_POI_VERIFICATION_PROVIDER=mock
POI_VERIFICATION_PROVIDER=mock
POI_PROVIDER=mock
POI_VERIFICATION_API_KEY=
POI_PROVIDER_API_KEY=
GOOGLE_PLACES_API_KEY=
POI_PROVIDER_BASE_URL=https://places.googleapis.com
POI_PROVIDER_TIMEOUT_SECONDS=5
POI_PROVIDER_RESULT_LIMIT=5
POI_PROVIDER_MIN_CONFIDENCE=0.82

ROUTING_PROVIDER=mock
ROUTING_API_KEY=
OPENROUTESERVICE_API_KEY=
ROUTING_BASE_URL=https://api.openrouteservice.org
ROUTING_TIMEOUT_SECONDS=5
ROUTING_MAX_STOPS=10
ROUTING_SUPPORTED_MODES=walking,car_taxi
ROUTING_FALLBACK_BEHAVIOR=mock

TICKETING_PROVIDER=mock
TICKETING_API_KEY=
TICKETING_BASE_URL=https://example.test
TICKETING_TIMEOUT_SECONDS=5

AFFILIATE_PROVIDER=mock
AFFILIATE_API_KEY=
AFFILIATE_BASE_URL=https://example.test/books
AFFILIATE_TIMEOUT_SECONDS=5

TTS_PROVIDER=mock
TTS_API_KEY=
TEXT_TO_SPEECH_API_KEY=
TTS_BASE_URL=https://example.test/tts
TTS_TIMEOUT_SECONDS=5

ENABLE_DURABLE_USAGE_CONTROLS=true
ANONYMOUS_ITINERARY_GENERATIONS_PER_MINUTE=5
ANONYMOUS_ITINERARY_GENERATIONS_PER_DAY=50
REGISTERED_USER_ITINERARY_GENERATIONS_PER_MINUTE=15
REGISTERED_USER_ITINERARY_GENERATIONS_PER_DAY=100
SUBSCRIBER_CHAT_MESSAGES_PER_MINUTE=30
SUBSCRIBER_CHAT_MESSAGES_PER_DAY=100
VECTOR_SEARCH_MAX_RESULTS=20
POI_VERIFICATION_MAX_BATCH_SIZE=25
TICKETING_LOOKUP_MAX_REQUESTS_PER_ITINERARY=10
ITINERARY_GENERATION_MAX_DAYS=7
PROVIDER_DAILY_REQUEST_CEILING=100
PROVIDER_DAILY_COST_CEILING_USD=0
USAGE_COUNTER_RETENTION_DAYS=90
